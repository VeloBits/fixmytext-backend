# FastAPI + ASGI
fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic-settings==2.14.1

# AI / keyword extraction
groq==1.2.0
yake==0.7.3

# Emoji (used by some AI tools)
emoji==2.15.0

# Redis (for rate limiting + arq task queue)
redis[hiredis]==7.4.0
# arq installed via --no-deps in Dockerfile to bypass its redis<6 constraint (API-compatible with redis 7.x)

# Structured logging
python-json-logger==4.1.0

# Testing
pytest==9.0.3
pytest-asyncio>=1.3.0
httpx==0.28.1
pytest-cov==7.1.0

# Observability
sentry-sdk[fastapi]==2.18.0
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-exporter-otlp-proto-http==1.34.1
opentelemetry-instrumentation-logging==0.55b1

# JWKS cache (shared security)
cachetools>=5.5

# PyJWT for token verification
PyJWT[crypto]==2.13.0

# Shared internal package
-e ./shared
