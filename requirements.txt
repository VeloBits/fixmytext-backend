# YAML support
PyYAML==6.0.3  # PyYAML: used by app/services/text_service.py

# Regex with timeout support — ReDoS protection for user-supplied patterns
regex>=2026.5.9

# FastAPI + ASGI server
fastapi==0.136.1
uvicorn[standard]==0.46.0

# Settings management
pydantic-settings==2.14.1

# Email validation (used by Pydantic EmailStr)
email-validator==2.3.0

# AI / keyword extraction
groq==1.2.0
yake==0.7.3

# Emoji handling
emoji==2.15.0

# Database (async SQLAlchemy + PostgreSQL)
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0
alembic==1.18.4

# Authentication
PyJWT[crypto]==2.12.1
bcrypt>=5.0.0
python-multipart==0.0.29

# Payments
razorpay==2.0.1

# Redis (optional — for distributed rate limiting)
redis[hiredis]==7.4.0

# Structured logging
python-json-logger==4.1.0

# Email (async SMTP for dev; prod provider TBD)
aiosmtplib==5.1.0

# Testing
pytest==9.0.3
pytest-asyncio>=1.3.0
httpx==0.28.1
pytest-cov==7.1.0
aiosqlite>=0.22.1  # in-memory async DB for query-count regression tests

# Observability — Sentry error tracking
sentry-sdk[fastapi]==2.18.0

# Observability — OpenTelemetry log shipping (Grafana Cloud / OTLP HTTP)
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1
opentelemetry-exporter-otlp-proto-http==1.34.1
opentelemetry-instrumentation-logging==0.55b1
