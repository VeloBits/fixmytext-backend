# YAML support
PyYAML==6.0.3  # PyYAML: used by app/services/text_service.py

# Regex with timeout support — ReDoS protection for user-supplied patterns
regex>=2024.5.15

# FastAPI + ASGI server
fastapi==0.136.1
uvicorn[standard]==0.34.0

# Settings management
pydantic-settings==2.2.0

# Email validation (used by Pydantic EmailStr)
email-validator==2.1.1

# AI / keyword extraction
groq==0.13.1
yake==0.4.8

# Emoji handling
emoji==2.12.0

# Database (async SQLAlchemy + PostgreSQL)
sqlalchemy[asyncio]==2.0.49
asyncpg==0.29.0
alembic==1.13.0

# Authentication
PyJWT[crypto]==2.12.1
bcrypt>=4.0
python-multipart==0.0.28

# Payments
setuptools==78.1.1  # Required by razorpay (pkg_resources)
razorpay==1.4.2

# Redis (optional — for distributed rate limiting)
redis[hiredis]>=5.0

# Structured logging
python-json-logger==2.0.7

# Email (async SMTP for dev; prod provider TBD)
aiosmtplib>=3.0

# Testing
pytest==9.0.3
pytest-asyncio>=1.0
httpx==0.28.1
pytest-cov==6.1.1
aiosqlite>=0.20  # in-memory async DB for query-count regression tests
